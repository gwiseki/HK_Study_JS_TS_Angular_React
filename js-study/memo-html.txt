li 태그는 list의 약자, 목록을 만드는 태그.
이 태그는 단독으로 쓰이지 않으며 <ul></li> 혹은 <ol></ol> 태그 내부에 들어간다.

<ol> 태그는 번호를 메겨 순서가 있는 (ordered list) 목록
<ul> 태그는 순서없이 모양으로 (unordered list) 목록
<li> 태그는 단순히 리스트 나열 뿐만 아니라 메뉴등을 만들때도 사용
이중 리스트를 만들기 위해서는 <ul>이나 <ol>하위에 다시 <ul>이나 <ol> 태그를 사용

